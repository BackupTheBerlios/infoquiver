################################################
## IBaseWebService.vm
## Author: netseeker aka Michael Manske
## Created on: 04/03/2004
################################################


#if ($database.Package)
  #set ($package = $database.Package)
#else
  #set ($package = $targetPackage)
#end

package net.sf.iquiver.remote.soap;

import java.util.List;
import java.util.Map;
import org.apache.torque.om.SimpleKey;

/**
#if ($addTimeStamp)
 * The skeleton for this class was autogenerated by Torque on:
 *
 * [$now]
 *
 */
#end
public interface IBaseWebService
{
	#foreach ($tbl in $database.Tables)
	  	#if (!$tbl.isForReferenceOnly())	    
			#set ( $objname = "${package}.${tbl.JavaName}" )
			public $objname doSelect${tbl.JavaName}($objname object);
			public List doSelect${tbl.JavaName}(Map criteria);
			public $objname retrieve${tbl.JavaName}ByPk(SimpleKey key);
			public $objname doInsert${tbl.JavaName}($objname object);
			public void doDelete${tbl.JavaName}($objname object);
			public void doDelete${tbl.JavaName}(Map criteria);
		#end  		
	#end	
}
