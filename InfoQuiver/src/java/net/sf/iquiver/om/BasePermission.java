package net.sf.iquiver.om;


import java.sql.Connection;
import java.util.ArrayList;
import java.util.Collections;
import java.util.List;

import org.apache.commons.lang.ObjectUtils;
import org.apache.torque.TorqueException;
import org.apache.torque.om.BaseObject;
import org.apache.torque.om.NumberKey;
import org.apache.torque.om.ObjectKey;
import org.apache.torque.om.SimpleKey;
import org.apache.torque.util.Transaction;


/**
 * This class was autogenerated by Torque on:
 *
 * [Tue Apr 13 20:47:11 CEST 2004]
 *
 * You should not use this class directly.  It should not even be
 * extended all references should be to Permission
 */
public abstract class BasePermission extends BaseObject
{
    /** The Peer class */
    private static final PermissionPeer peer =
        new PermissionPeer();

        
    /** The value for the permissionId field */
    private int permissionId;
      
    /** The value for the permissionName field */
    private String permissionName;
      
    /** The value for the permissionDesc field */
    private String permissionDesc;
  
    
    /**
     * Get the PermissionId
     *
     * @return int
     */
    public int getPermissionId()
    {
        return permissionId;
    }

                        
    /**
     * Set the value of PermissionId
     *
     * @param v new value
     */
    public void setPermissionId(int v) 
    {
    
                  if (this.permissionId != v)
              {
            this.permissionId = v;
            setModified(true);
        }
    
          
              }
  
    /**
     * Get the PermissionName
     *
     * @return String
     */
    public String getPermissionName()
    {
        return permissionName;
    }

                        
    /**
     * Set the value of PermissionName
     *
     * @param v new value
     */
    public void setPermissionName(String v) 
    {
    
                  if (!ObjectUtils.equals(this.permissionName, v))
              {
            this.permissionName = v;
            setModified(true);
        }
    
          
              }
  
    /**
     * Get the PermissionDesc
     *
     * @return String
     */
    public String getPermissionDesc()
    {
        return permissionDesc;
    }

                        
    /**
     * Set the value of PermissionDesc
     *
     * @param v new value
     */
    public void setPermissionDesc(String v) 
    {
    
                  if (!ObjectUtils.equals(this.permissionDesc, v))
              {
            this.permissionDesc = v;
            setModified(true);
        }
    
          
              }
  
         
                
    private static List fieldNames = null;

    /**
     * Generate a list of field names.
     *
     * @return a list of field names
     */
    public static synchronized List getFieldNames()
    {
        if (fieldNames == null)
        {
            fieldNames = new ArrayList();
              fieldNames.add("PermissionId");
              fieldNames.add("PermissionName");
              fieldNames.add("PermissionDesc");
              fieldNames = Collections.unmodifiableList(fieldNames);
        }
        return fieldNames;
    }

    /**
     * Retrieves a field from the object by name passed in as a String.
     *
     * @param name field name
     * @return value
     */
    public Object getByName(String name)
    {
          if (name.equals("PermissionId"))
        {
                return new Integer(getPermissionId());
            }
          if (name.equals("PermissionName"))
        {
                return getPermissionName();
            }
          if (name.equals("PermissionDesc"))
        {
                return getPermissionDesc();
            }
          return null;
    }
    
    /**
     * Retrieves a field from the object by name passed in
     * as a String.  The String must be one of the static
     * Strings defined in this Class' Peer.
     *
     * @param name peer name
     * @return value
     */
    public Object getByPeerName(String name)
    {
          if (name.equals(PermissionPeer.PERMISSION_ID))
        {
                return new Integer(getPermissionId());
            }
          if (name.equals(PermissionPeer.PERMISSION_NAME))
        {
                return getPermissionName();
            }
          if (name.equals(PermissionPeer.PERMISSION_DESC))
        {
                return getPermissionDesc();
            }
          return null;
    }

    /**
     * Retrieves a field from the object by Position as specified
     * in the xml schema.  Zero-based.
     *
     * @param pos position in xml schema
     * @return value
     */
    public Object getByPosition(int pos)
    {
            if (pos == 0)
        {
                return new Integer(getPermissionId());
            }
              if (pos == 1)
        {
                return getPermissionName();
            }
              if (pos == 2)
        {
                return getPermissionDesc();
            }
              return null;
    }
     
    /**
     * Stores the object in the database.  If the object is new,
     * it inserts it; otherwise an update is performed.
     *
     * @throws Exception
     */
    public void save() throws Exception
    {
          save(PermissionPeer.getMapBuilder()
                .getDatabaseMap().getName());
      }

    /**
     * Stores the object in the database.  If the object is new,
     * it inserts it; otherwise an update is performed.
       * Note: this code is here because the method body is
     * auto-generated conditionally and therefore needs to be
     * in this file instead of in the super class, BaseObject.
       *
     * @param dbName
     * @throws TorqueException
     */
    public void save(String dbName) throws TorqueException
    {
        Connection con = null;
          try
        {
            con = Transaction.begin(dbName);
            save(con);
            Transaction.commit(con);
        }
        catch(TorqueException e)
        {
            Transaction.safeRollback(con);
            throw e;
        }
      }

      /** flag to prevent endless save loop, if this object is referenced
        by another object which falls in this transaction. */
    private boolean alreadyInSave = false;
      /**
     * Stores the object in the database.  If the object is new,
     * it inserts it; otherwise an update is performed.  This method
     * is meant to be used as part of a transaction, otherwise use
     * the save() method and the connection details will be handled
     * internally
     *
     * @param con
     * @throws TorqueException
     */
    public void save(Connection con) throws TorqueException
    {
          if (!alreadyInSave)
        {
            alreadyInSave = true;


  
            // If this object has been modified, then save it to the database.
            if (isModified())
            {
                if (isNew())
                {
                    PermissionPeer.doInsert((Permission) this, con);
                    setNew(false);
                }
                else
                {
                    PermissionPeer.doUpdate((Permission) this, con);
                }
            }

                      alreadyInSave = false;
        }
      }


                    
      /**
     * Set the PrimaryKey using ObjectKey.
     *
     * @param  permissionId ObjectKey
     */
    public void setPrimaryKey(ObjectKey key)
        
    {
            setPermissionId(((NumberKey) key).intValue());
        }

    /**
     * Set the PrimaryKey using a String.
     *
     * @param key
     */
    public void setPrimaryKey(String key) 
    {
            setPermissionId(Integer.parseInt(key));
        }

  
    /**
     * returns an id that differentiates this object from others
     * of its class.
     */
    public ObjectKey getPrimaryKey()
    {
          return SimpleKey.keyFor(getPermissionId());
      }

 

    /**
     * Makes a copy of this object.
     * It creates a new object filling in the simple attributes.
       * It then fills all the association collections and sets the
     * related objects to isNew=true.
       */
      public Permission copy() throws TorqueException
    {
        return copyInto(new Permission());
    }
  
    protected Permission copyInto(Permission copyObj) throws TorqueException
    {
          copyObj.setPermissionId(permissionId);
          copyObj.setPermissionName(permissionName);
          copyObj.setPermissionDesc(permissionDesc);
  
                    copyObj.setPermissionId(0);
                        
        
        return copyObj;
    }

    /**
     * returns a peer instance associated with this om.  Since Peer classes
     * are not to have any instance attributes, this method returns the
     * same instance for all member of this class. The method could therefore
     * be static, but this would prevent one from overriding the behavior.
     */
    public PermissionPeer getPeer()
    {
        return peer;
    }

    public String toString()
    {
        StringBuffer str = new StringBuffer();
        str.append("Permission:\n");
        str.append("PermissionId = ")
           .append(getPermissionId())
           .append("\n");
        str.append("PermissionName = ")
           .append(getPermissionName())
           .append("\n");
        str.append("PermissionDesc = ")
           .append(getPermissionDesc())
           .append("\n");
        return(str.toString());
    }
}
